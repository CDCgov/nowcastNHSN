% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fetch_reporting_data_epidatr.R
\name{fetch_reporting_data_epidatr}
\alias{fetch_reporting_data_epidatr}
\title{Fetch NHSN data formatted for reporting triangle construction}
\usage{
fetch_reporting_data_epidatr(
  signal = "confirmed_admissions_covid_ew_prelim",
  geo_type = "state",
  geo_values = "*",
  reference_dates = "*",
  report_dates = "*",
  ...
)
}
\arguments{
\item{signal}{Character string specifying the NHSN signal to fetch.
Default is \code{"confirmed_admissions_covid_ew_prelim"}.}

\item{geo_type}{Character string specifying geographic resolution.
Options: \code{"state"}, \code{"hhs"}, \code{"nation"}. Default is \code{"state"}.}

\item{geo_values}{Character vector of geography codes,
or \code{"*"} for all geographies. Default is \code{"*"}.}

\item{reference_dates}{A timeset object (e.g., \code{epirange(202401, 202404)})
or \code{"*"} for all available dates. Default is \code{"*"}.}

\item{report_dates}{A timeset object for specific report dates, or \code{"*"}
for most all. Default is \code{"*"}.}

\item{...}{Additional arguments passed to \code{epidatr::pub_covidcast()} via
\code{\link[=fetch_nhsn_data]{fetch_nhsn_data()}}, such as \code{fetch_args} for controlling caching behavior.}
}
\value{
A data frame in long format suitable for reporting triangle construction
with columns:
\itemize{
\item \code{reference_date}: Date of the event (epiweek-ending Saturday)
\item \code{report_date}: Date when data was reported (epiweek-ending Saturday)
\item \code{count}: Number of confirmed admissions
\item \code{location}: Geographic identifier
\item \code{signal}: Signal name
}
}
\description{
Wrapper around \code{\link[=fetch_nhsn_data]{fetch_nhsn_data()}} that reshapes and converts the output
to a long format suitable for reporting triangle construction.
Uses \code{reference_week_day = 7} by default to return Saturday week-ending
dates, matching both MMWR epiweek and forecast hub conventions.
}
\concept{data_fetching}
