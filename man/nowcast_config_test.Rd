% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/config.R
\name{nowcast_config_test}
\alias{nowcast_config_test}
\title{Create a test configuration for quick pipeline validation}
\usage{
nowcast_config_test(location = "ca", draws = 10L, output_dir = NULL, ...)
}
\arguments{
\item{location}{Character. Single location code to test with.
Default is \code{"ca"} (California).}

\item{draws}{Integer. Number of draws (reduced for speed).
Default is \code{10}.}

\item{output_dir}{Character. Output directory. Default is \code{NULL}
(no file output, just returns results).}

\item{...}{Additional arguments passed to \code{\link[=nowcast_config]{nowcast_config()}}.}
}
\value{
A \code{nowcast_config} object with test-friendly defaults.
}
\description{
Creates a minimal configuration for quickly testing the nowcast pipeline.
Uses reduced draws, a single state, and fast settings to enable rapid
iteration during development.
}
\examples{
# Quick test config
config <- nowcast_config_test()
print(config)

# Test with a different state
config <- nowcast_config_test(location = "ny")

# Test with file output
config <- nowcast_config_test(output_dir = "test_output")
}
